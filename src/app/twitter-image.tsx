// X reads its own tag rather than falling back to the Open Graph one, so the
// same card is exported again here. Same art, same words — only the meta tag
// it lands in differs.
export { default, alt, size, contentType } from "./opengraph-image";
